# DataNexa v0.8.0

[English Release Notes](https://github.com/MingoZacwu/DataNexa/blob/v0.8.0/docs/RELEASE_NOTES.en.md)

## 版本亮点

- 新增 JDBC 支持(技术预览)：通过 Maven 坐标安装任意 JDBC 驱动，即可连接更多企业数据库与国产数据库。
- 新增“安全”设置页，提供拦截请求统计与访问令牌自动熔断。
- 提供 DataNexa 托管 Java 运行时，JDBC 功能开箱即用，也可改用本机已有的外部 Java 运行时。
- **注意**：审计日志的保留策略由“最大保留条数”调整为“保存天数上限”，详见下方[调整与改进](#调整与改进)。

## 新增功能

### JDBC 支持(技术预览)

- JDBC 操作运行在独立 Java sidecar 进程中，与现有后端隔离；SQLite、MySQL 和 PostgreSQL 连接仍使用原生路径，不受影响。
- 新增“驱动管理”设置页：
  - 输入 Maven 坐标即可安装任意 JDBC 驱动及其传递依赖，不限制数据库品牌，支持配置 Maven 镜像源；
  - 下载的驱动会经过大小与 SHA-256 校验，并保存为不可变的版本化 Driver Bundle，支持并存、更新与删除；
  - 支持导入本地 JAR，并提供 Maven 下载缓存的查看与清理。
- 新增“JDBC”连接类型：选择已安装的驱动并填写 JDBC URL 即可创建连接，支持连接测试、诊断与连接导入/导出。
- JDBC 查询沿用现有的只读策略、访问控制、审计、超时与返回限制；未知方言或无法确认安全的语句会被拒绝。
- JDBC 凭证与 URL 在错误信息和诊断输出中脱敏。

### DataNexa 托管 Java 运行时

- 在设置页一键下载 DataNexa 托管的 Java 运行时，下载后自动校验完整性，无需单独安装 Java。
- 也可以在设置中选择本机已有的外部 Java 运行时。
- 自动检测托管运行时的新版本，更新提醒以侧边栏卡片展示。

### 安全设置页

- 新增“安全”设置页：展示近 24 小时被拦截的请求统计、纵深防御概览，并根据 Bearer 鉴权状态给出提醒。
- 访问令牌自动熔断：开启 Bearer 鉴权后，若某个令牌在计数窗口内被拒绝的请求达到阈值，将自动禁用该令牌；触发熔断与手动恢复均记入审计日志，重新启用的令牌从新的计数窗口开始。

### 存储与性能

- 新增“存储与性能”设置页：按 Java 运行时、JDBC 驱动、Maven 仓库、审计与日志等类别，展示本地数据占用明细及其实际存储路径。
- 支持一键清理可安全删除的缓存项目(如 Maven 下载缓存、旧版运行时)以释放空间，已安装的驱动、审计日志和配置不受影响。
- 支持在系统文件管理器中直接打开 DataNexa 数据目录。

## 调整与改进

- **审计日志保留策略变更**：审计日志的清理方式由“最大保留条数”调整为“保存天数上限”，可选 3/7/15/30 天，默认保留 7 天。升级到本版本后，早于保留天数的历史审计记录将被自动清理。**如有长期留存审计记录的需要，请在升级前做好日志备份，并在设置中选择更长的保留天数。**
- 连接导入更稳健：导入文件中无效或不可用的条目会被跳过，并明确提示导入结果，不再影响其余条目。
- 应用与 Java 运行时的更新检查结果会被缓存，避免重复请求。

## 问题修复

- 改进 Claude Desktop 及旧版 MCP 客户端的兼容性：使用更严格的工具输入 Schema、支持 MCP 协议版本 2025-03-26 与 `structuredContent` 返回，并兼容缺少部分协议头的旧客户端。
- 修复内容不足一屏时仍显示滚动渐隐与滚动条的问题，现在仅在内容可滚动时显示。

## 升级须知与问题反馈

- 本次版本更新中改动内容较多。我们无法保证针对所有功能与使用场景都进行了完整测试，JDBC 支持也无法逐一验证所有数据库与驱动版本的兼容性。如果在升级后遇到问题，欢迎在 [GitHub Issues](https://github.com/MingoZacwu/DataNexa/issues) 提交反馈，并尽量附上问题的复现步骤与环境信息(操作系统、数据库类型与版本、驱动名称与版本)。
- 如需协助定位问题，可以在“设置 → 关于”页面连续点按版本号区域 5 次，打开隐藏的调试日志选项；开启后复现问题，再在设置中打开日志文件夹获取日志文件。日志内容已经过脱敏处理，不会记录明文凭据，随 Issue 一并提交可以帮助我们更快定位问题。

## 安装与使用说明

- macOS 版本要求 macOS 15.0 或更高版本。

- macOS 应用使用 Developer ID 签名，但尚未进行 Apple 公证。首次启动时，macOS 可能显示 Gatekeeper 安全提示。您可以在终端中执行以下命令来解除警告：

```shell
sudo xattr -d com.apple.quarantine /Applications/DataNexa.app
```

- Windows 安装包暂未进行 Authenticode 代码签名，Microsoft Defender SmartScreen 可能显示安全提示。
